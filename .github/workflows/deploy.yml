name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          # trustnodelab.github.io — VITE_BASE must be "/" (org Pages, no subpath)
          VITE_BASE: ${{ vars.VITE_BASE || '/' }}
          # Cloudflare assets worker (trustnode-assets) — set this repo variable
          # to your worker URL (e.g. https://trustnode-assets.<account>.workers.dev)
          # after deploying workers/assets. News photos + ML model are proxied there.
          VITE_ASSETS_URL: ${{ vars.VITE_ASSETS_URL || '' }}
      - name: Deploy to gh-pages branch
        run: |
          cp -r dist ../dist-copy
          git fetch origin gh-pages 2>/dev/null || git branch gh-pages
          git checkout gh-pages 2>/dev/null || git checkout -b gh-pages
          git rm -rf . 2>/dev/null || true
          cp -r ../dist-copy/* .
          git add -A
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git commit -m "Deploy to GitHub Pages"
          git push origin gh-pages --force
